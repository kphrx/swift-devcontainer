{
    "id": "swift-format",
    "version": "1.2.0-alpha.7",
    "name": "swift-format",
    "description": "Formatting technology for Swift source code",
    "options": {
        "branchName": {
            "type": "string",
            "description": "Specify branch of swift-format, or 'auto' to set from `swift --version`.",
            "proposals": ["main", "auto"],
            "default": "auto"
        },
        "forceBuild": {
            "type": "boolean",
            "description": "If set to `false` and `branchName` set to `auto`, don't build when swift-format included toolchains. default: `false`",
            "default": false
        }
    },
    "containerEnv": {
        "PATH": "/root/.swiftpm/bin:${PATH}"
    },
    "installsAfter": [
        "ghcr.io/devcontainers/features/common-utils",
        "ghcr.io/devcontainers/features/git"
    ]
}
